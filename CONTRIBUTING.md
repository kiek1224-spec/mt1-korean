# 기여 안내

오역 한 줄 제보부터 빌드 코드 수정까지 모두 환영합니다. 검수 인력이 적어서, **확인 자료를 같이 보내 주시면** 빨리 반영할 수 있습니다.

## 1. 오역·미번역 제보 (Issue)

[Issue](../../issues/new/choose) 에서 「번역 제보」 양식을 써 주세요.
- 어디서 봤는지 (장소·상황)
- 화면 스크린샷 (가능하면 원판 화면도)
- 가능하면 Mesen 세이브스테이트(`.mss`) — **롬 파일은 절대 올리지 마세요**

## 2. 번역 고치기 (Pull Request)

가장 쉬운 방법: GitHub 에서 파일을 열고 연필 아이콘으로 고친 뒤 PR 을 보내면 됩니다.

### `script_ko.txt` (대사)
```
@8320 [0:0]
JP: 원문 (참고용, 고치지 않음)
KO: 번역문   ← 이 줄만 고칩니다
```
- `<..>` 로 된 제어코드는 **순서와 개수를 그대로** 둡니다 (`<FD>` 줄바꿈, `<FE>` 종료, `<F9>` 주인공 이름 등).
- 한 화면(22열×4줄)에 **서로 다른 글자 63자 이하**.
- 「※꼬리공유」 표시가 있는 블록은 앞 메시지를 번역하면 함께 덮이므로 비워 둡니다.

### `ui_ko.txt` (UI 문자열 — 제자리 치환)
```
주소<TAB>원문바이트수<TAB>번역
```
- 번역이 **원문 바이트 수를 넘으면 빌드가 멈춥니다.** 정적 폰트(`static_font.txt`)에 있는 음절은 1바이트, 그 밖의 음절은 2바이트입니다.
- 빈칸은 `<59>`, 제어코드는 `<XX>` 로 씁니다. 줄 **중간**에 `<FB>` `<FE>` `<FF>` 는 넣을 수 없습니다.
- ★**한글 이스케이프 음절이 223/224종으로 거의 찼습니다.** 새 음절을 쓰면 한도를 넘을 수 있으니,
  이미 쓰고 있는 음절(또는 정적 폰트 음절)로 표현해 주세요. 한도를 넘으면 빌드가 알려 줍니다.
- 주소를 한 바이트라도 틀리면 게임 데이터(포인터)를 덮어 화면이 깨집니다. `check_uistr.py` 가 잡아 줍니다.

## 3. PR 에 꼭 붙여 주세요

1. **빌드 로그** — `python make.py vNN 태그 --env MT1_M195=1` 의 마지막 결과가 「통과」인 부분
   (빌드 방법: [README 4장](README.md#4-직접-빌드하기))
2. **고친 화면의 스크린샷**
3. 롬(`.nes`)·세이브스테이트·zip 은 커밋하지 않습니다 (`.gitignore` 로 막혀 있습니다)

빌드 환경이 없으면 번역 파일만 고쳐서 보내 주셔도 됩니다. 이 경우 검수자가 빌드해 확인합니다.

## 4. 코드를 고칠 때

- 먼저 [`docs/mt1-hangul-memory.md`](docs/mt1-hangul-memory.md) 와 [`작업롬파일/VERSIONS.md`](작업롬파일/VERSIONS.md) 를 읽어 주세요.
  같은 함정에 여러 번 빠진 기록이 있습니다.
- 경로는 `mtpaths.py` 에서만 정합니다. 스크립트에 절대경로를 넣지 마세요.
- 도움이 되는 도구
  - `scan_neighbors.py <롬>` — 번역 항목 앞뒤에 남은 원문 조각 찾기
  - `savestate_trace.py <롬> <세이브> <출력.png>` — 세이브를 헤드리스로 올려 화면·뱅크 전환 기록
- **캐릭터 상태창의 UI 침범 문제는 「당분간 그대로 둔다」로 정해져 있습니다.** 고치고 싶으면 먼저 Issue 로 이야기해 주세요.
